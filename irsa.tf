# IRSA (IAM Roles for Service Accounts) role for the EBS CSI driver.
# This gives ONLY the ebs-csi-controller-sa service account (not the whole
# node) the EC2/EBS permissions it needs to create and attach volumes.

module "ebs_csi_irsa_role" {
  source  = "terraform-aws-modules/iam/aws//modules/iam-role-for-service-accounts-eks"
  version = "~> 5.0"

  role_name             = "${var.cluster_name}-ebs-csi-driver"
  attach_ebs_csi_policy = true

  oidc_providers = {
    main = {
      provider_arn               = module.eks.oidc_provider_arn
      namespace_service_accounts = ["kube-system:ebs-csi-controller-sa"]
    }
  }

  tags = var.tags
}
